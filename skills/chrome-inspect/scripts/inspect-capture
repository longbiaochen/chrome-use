#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
RUNTIME_ROOT="$("$SCRIPT_DIR/resolve_runtime_root.sh")"
exec node "$RUNTIME_ROOT/scripts/inspect_capture.mjs" "$@"
